import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['drive.google.com', 'example.com'], // Add more domains as needed
  },
  /* config options here */
};

export default nextConfig;
